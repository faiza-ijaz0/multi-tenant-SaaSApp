# supabase/migrations

Version-controlled SQL migrations — the source of truth for the database schema. Applied to the `SignalBoard Development` Supabase project through the Supabase MCP `apply_migration` tool, never edited directly in Studio.

Naming: `<timestamp>_<description>.sql`. First migration lands in Phase 1.
